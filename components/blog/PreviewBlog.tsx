import React from "react";

type PreviewBlogTypes = {
  previewTitle: string;
  previewContent: string;
};
const PreviewBlog = ({ previewTitle, previewContent }: PreviewBlogTypes) => {
  return <div>PreviewBlog</div>;
};

export default PreviewBlog;
